The data are too large to fit on my laptop, so I need a symbolic link to an external hard drive.

Make a symbolic link to the sandbox folder on the hard drive:
`ln -s /Volumes/My\ Book/sandbox/ data/sandbox`
